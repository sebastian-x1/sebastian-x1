package farkle;

/**
 * The {@code ComputerDifficulty} enumeration represents the difficulty level
 * at which a {@code ComputerPlayer} will make decisions that impact a {@code HumanPlayer}.
 */
public enum ComputerDifficulty {
	EASY,
	HARD
}
